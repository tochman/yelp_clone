
<%= form_for [@restaurant, @comment]  do |f| %>
  <%= f.label :body %>:
  <%= f.text_area :body %>
  <%= f.label :rating %>
  <%= f.select :rating, [1,2,3,4,5] %>
  <%= f.submit 'Add comment' %>
<% end %>